server:
  port: 8085

spring:
  application:
    name: network-mock-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.mcp.network: DEBUG
